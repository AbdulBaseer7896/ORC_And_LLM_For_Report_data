Flask==2.2.2
Werkzeug==2.2.2
paddleocr==2.6.0  # or the version you are using
paddlepaddle==2.5.0  # or the version you need (make sure it’s compatible with paddleocr)
requests==2.28.2
gunicorn==20.1.0
